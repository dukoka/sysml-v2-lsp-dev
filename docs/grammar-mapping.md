# G4 与 Langium 语法规则映射表

本文档预留用于记录 G4（ANTLR）语法规则与 Langium 语法规则的对应关系，便于未来 G4 到位后的对照与半自动转换。

## 映射表（待填写）

| G4 规则名 | Langium 规则名 | AST 类型 |
|-----------|----------------|----------|
| （待 G4 文件提供后填写） | | |

## 说明

- 当用户提供 G4 语法文件时，在此表中维护规则名与 AST 类型的对应关系
- 用于验证 Langium 语法与 G4 的覆盖一致性
- 可选：基于此表实现 `scripts/g4-to-langium.ts` 半自动转换脚本
